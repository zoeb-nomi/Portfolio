---
import Base from '../../layouts/Base.astro';
import Section from '../../components/Section.astro';
import PullQuote from '../../components/PullQuote.astro';
import CTASlab from '../../components/CTASlab.astro';
import { meta, cta, home, writingReflection as wr } from '../../data/copy';

const articleLd = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: wr.title,
  description: meta.writingReflection.description,
  author: { '@type': 'Person', name: 'Zoeb Nomi', url: 'https://www.zoebnomi.com' },
  url: 'https://www.zoebnomi.com/writing/eval-harness-at-my-own-reflection/',
  mainEntityOfPage: 'https://www.zoebnomi.com/writing/eval-harness-at-my-own-reflection/',
  datePublished: wr.date,
  dateModified: wr.date,
  about: 'AI search evaluation, LLM-as-judge validation, self-published claims',
  isPartOf: { '@type': 'WebSite', name: 'Zoeb Nomi', url: 'https://www.zoebnomi.com' },
};
---

<Base
  title={meta.writingReflection.title}
  description={meta.writingReflection.description}
  path="/writing/eval-harness-at-my-own-reflection/"
  ogImage="/og/crosssource.png"
  ogType="article"
>
  <script type="application/ld+json" set:html={JSON.stringify(articleLd)} is:inline />

  <section class="masthead">
    <div class="masthead-inner">
      <aside class="margin-col" aria-label="Article kicker">
        <p class="kicker">{wr.kicker}</p>
      </aside>
      <div class="body-col">
        <h1 class="page-h1">{wr.title}</h1>
        <p class="standfirst">{wr.dek}</p>
        <p class="byline tabular">{wr.date} · {wr.readingTime} read</p>
      </div>
    </div>
  </section>

  <Section mark={wr.s1.mark} label={wr.s1.label}>
    {wr.s1.paras.map((p) => <p class="prose">{p}</p>)}
  </Section>

  <Section mark={wr.s2.mark} label={wr.s2.label}>
    {wr.s2.paras.map((p) => <p class="prose">{p}</p>)}
  </Section>

  <Section mark={wr.s3.mark} label={wr.s3.label}>
    {wr.s3.paras.map((p) => <p class="prose">{p}</p>)}
  </Section>

  <Section mark={wr.s4.mark} label={wr.s4.label}>
    {wr.s4.paras.map((p) => <p class="prose">{p}</p>)}
    <div class="lesson">
      <PullQuote rule="red">{wr.s4.lessonQuote}</PullQuote>
    </div>
  </Section>

  <Section mark={wr.s5.mark} label={wr.s5.label}>
    {wr.s5.paras.map((p) => <p class="prose">{p}</p>)}
    <p class="prose links-line">
      {wr.footer.map((link, i) => (
        <>
          {i > 0 && <span class="sep" aria-hidden="true"> · </span>}
          <a href={link.href} target={link.href.startsWith('http') ? '_blank' : undefined} rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}>{link.label}</a>
        </>
      ))}
    </p>
  </Section>

  <CTASlab
    marginLines={[home.eyebrow.toUpperCase()]}
    body={home.ctaSlabBody}
    buttonLabel="View the repo →"
    buttonHref={wr.repoHref}
    secondaryLabel={cta.bookACall.label}
    secondaryHref={cta.bookACall.href}
  />
</Base>

<style>
  .masthead-inner {
    display: grid;
    grid-template-columns: var(--margin-col) 1fr;
    border-bottom: 1px solid var(--rule-strong);
  }

  .margin-col {
    border-right: 1px solid var(--rule);
    padding: var(--s-9) var(--s-5) var(--s-9) var(--gutter);
  }

  .kicker {
    font-family: var(--font-mono);
    font-size: var(--t-label);
    letter-spacing: .12em;
    text-transform: uppercase;
    color: var(--ink-muted);
  }

  .body-col {
    padding: var(--s-9) var(--gutter) var(--s-11) var(--body-inset);
  }

  .page-h1 {
    font-family: var(--font-display);
    font-size: var(--t-page);
    line-height: 1.02;
    letter-spacing: -.015em;
    color: var(--ink);
    margin-bottom: var(--s-6);
    max-width: 24ch;
  }

  .standfirst {
    font-family: var(--font-body);
    font-size: var(--t-lead);
    line-height: 1.62;
    color: var(--ink-body);
    max-width: 62ch;
    margin-bottom: var(--s-4);
  }

  .byline {
    font-family: var(--font-mono);
    font-size: var(--t-caption);
    color: var(--ink-muted);
  }

  .prose {
    font-family: var(--font-body);
    font-size: var(--t-lead);
    line-height: 1.62;
    color: var(--ink-body);
    max-width: 68ch;
  }

  .prose + .prose {
    margin-top: var(--s-6);
  }

  .lesson {
    margin-top: var(--s-8);
  }

  .links-line {
    margin-top: var(--s-6);
    font-family: var(--font-mono);
    font-size: var(--t-caption);
  }

  .links-line a {
    color: var(--red-ink);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .links-line .sep {
    color: var(--ink-muted);
  }

  @media (max-width: 899px) {
    .masthead-inner {
      grid-template-columns: 1fr;
    }

    .margin-col {
      border-right: 0;
      border-bottom: 1px solid var(--rule);
      padding: var(--s-6) var(--gutter);
    }

    .body-col {
      padding: var(--s-7) var(--gutter);
    }
  }
</style>
